import React from "react";
import ReactDOM from "react-dom/client";
import StudioAroeiraOS from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <StudioAroeiraOS />
  </React.StrictMode>
);
